const retailers = state => state.retailers

const retailersMeta = state => state.meta

const retailer = state => state.retailer

const retailersLoading = state => state.retailersLoading

const retailerLoading = state => state.retailerLoading

const retailerSaveLoading = state => state.retailerSaveLoading

export default {
  retailer,
  retailers,
  retailersLoading,
  retailerLoading,
  retailerSaveLoading,
  retailersMeta
}
